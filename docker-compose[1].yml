version: "3.8"
services:
  amount-service:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - TZ=Asia/Kolkata
    # If you want to mount local code for development, uncomment volumes:
    # volumes:
    #   - ./:/app
